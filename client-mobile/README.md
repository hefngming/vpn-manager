# 🦞 小龙虾VPN - 移动客户端

基于 Flutter 的跨平台移动 VPN 客户端，支持 iOS 和 Android。

## ✨ 功能特性

- 🔐 **零配置** - 登录即用，无需手动配置节点
- ⚡ **一键连接** - 选择节点，点击连接
- 📊 **流量统计** - 实时显示流量使用情况
- 🔔 **连接状态** - 清晰的连接状态和时长显示
- 🎨 **精美 UI** - 深色主题，类似商业 VPN 体验

## 🚀 快速开始

### 环境要求
- Flutter SDK 3.0+
- Dart SDK 3.0+
- Android Studio / Xcode

### 安装依赖

```bash
cd client-mobile
flutter pub get
```

### 运行应用

```bash
# iOS 模拟器
flutter run -d ios

# Android 模拟器
flutter run -d android

# 或指定设备
flutter run -d <device-id>
```

### 构建发行版

```bash
# Android APK
flutter build apk --release

# Android App Bundle
flutter build appbundle --release

# iOS
flutter build ios --release
```

## 📁 项目结构

```
client-mobile/
├── lib/
│   ├── main.dart                 # 应用入口
│   ├── models/
│   │   └── user.dart            # 数据模型
│   ├── providers/
│   │   └── app_providers.dart   # 状态管理
│   ├── services/
│   │   └── api_service.dart     # API 服务
│   └── screens/
│       ├── splash_screen.dart   # 启动页
│       ├── login_screen.dart    # 登录
│       ├── register_screen.dart # 注册
│       └── main_screen.dart     # 主界面
├── pubspec.yaml
└── README.md
```

## 🔌 工作原理

```
┌─────────────────────────────────────────────────────────┐
│                    Flutter 客户端                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │     UI 层     │  │  状态管理     │  │   API 层     │  │
│  │  (Screens)   │  │  (Riverpod)  │  │   (Dio)      │  │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  │
│         │                  │                  │          │
│         │ 1. 用户选择节点   │                  │          │
│         │ ────────────────▶│                  │          │
│         │                  │                  │          │
│         │                  │ 2. 调用 API      │          │
│         │                  │ ────────────────▶│          │
│         │                  │                  │          │
│         │                  │ 3. 获取配置      │          │
│         │                  │ ◀────────────────│          │
│         │                  │                  │          │
│         │                  │ 4. 启动 VPN      │          │
│         │                  │ (需要原生集成)    │          │
│         │                  │                  │          │
└─────────┼──────────────────┼──────────────────┼──────────┘
          │                  │                  │
          ▼                  ▼                  ▼
     ┌─────────┐      ┌─────────┐      ┌─────────┐
     │ 用户界面 │      │ VPN 核心 │      │ 后端 API │
     └─────────┘      └─────────┘      └─────────┘
```

## 📝 TODO

- [ ] 集成 iOS NetworkExtension VPN
- [ ] 集成 Android VpnService
- [ ] 添加支付功能（应用内购买）
- [ ] 添加推送通知
- [ ] 添加节点测速功能

## 🔧 开发计划

### Phase 1: 基础框架 ✅
- [x] Flutter 项目搭建
- [x] 基础 UI 界面
- [x] 状态管理 (Riverpod)
- [x] API 集成

### Phase 2: VPN 核心
- [ ] iOS NetworkExtension 集成
- [ ] Android VpnService 集成
- [ ] 连接状态管理
- [ ] 流量统计

### Phase 3: 高级功能
- [ ] 应用内购买
- [ ] 推送通知
- [ ] 智能路由

## 🐛 已知问题

1. 目前只是 UI 框架，没有实际 VPN 功能
2. 需要集成原生 VPN 框架（iOS NetworkExtension / Android VpnService）

## 📄 许可证

MIT
